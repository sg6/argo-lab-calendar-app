apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: calendar-postgres
  namespace: calendar-app
spec:
  instances: 1

  storage:
    size: 5Gi

  bootstrap:
    initdb:
      database: calendar
      owner: calendar
      secret:
        name: calendar-db-app
